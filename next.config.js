/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/dayel-2',
  assetPrefix: '/dayel-2',
}

module.exports = nextConfig